org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Maven coordinates — shared by every publishable module.
# groupId is io.github.abdullajon1881 so Maven Central can auto-verify ownership
# against the GitHub account (no domain/DNS verification needed). Note: this is
# the publishing groupId only — the Kotlin package / Android namespace remain
# dev.nudgekit.* and are intentionally not renamed.
# Publishing dry-run only: artifacts are generated locally (publishToMavenLocal).
# NOT yet published to Maven Central, and signing is intentionally not configured.
nudgekitGroup=io.github.abdullajon1881
nudgekitVersion=0.2.0-alpha.1
