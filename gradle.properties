org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Maven coordinates — shared by every publishable module.
# Publishing dry-run only: artifacts are generated locally (publishToMavenLocal).
# NOT yet published to Maven Central, and signing is intentionally not configured.
nudgekitGroup=dev.nudgekit
nudgekitVersion=0.2.0-alpha.1
